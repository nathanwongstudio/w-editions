title: Default Page

columns:
  main:
    width: 1
    sections:
      fields:
        type: fields
        fields:
          text:
            type: layout
            size: huge
            extends: fields/layout