title: Home Page

tabs:
  content:
    columns:
      main:
        width: 1
        sections:
          fields:
            type: fields
            fields:
              text:
                extends: fields/layout

  media:
    sections:
      files:
        type: files
  security:
    icon: lock
    columns:
      - width: 1/3
        fields:
          pageProtect: fields/locked-pages
  
  settings:
    icon: cog
    extends: layouts/page-settings