title: Gallery
icon: 🏛

tabs:
  content:
    icon: edit
    columns:

      main:
        width: 1
        sections:
          portfolio:
            type: pages
            label: Gallery
            layout: cards
            size: small
            empty: No artwork yet.
            templates: gallery-artwork
            image:
              query: page.primaryImg.toFile
              back: white
              ratio: 1/1
  security:
    icon: lock
    columns:
      - width: 1/3
        fields:
          pageProtect: fields/locked-pages

