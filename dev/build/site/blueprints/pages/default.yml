title: Default Page

tabs:
  content:
    columns:
      main:
        width: 1
        sections:
          fields:
            type: fields
            fields:
              text:
                extends: fields/layout
  
  security:
    icon: lock
    columns:
      - width: 1/3
        fields:
          pageProtect: fields/locked-pages