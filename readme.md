# W Editions Github Repo

## Installation
Hello, in order to preview this site correctly, you need to do a few things:
1. clone the repo
2. install dependencies using `composer update`

you can also install the dependencies independently.